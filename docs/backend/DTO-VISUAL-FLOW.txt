```
╔══════════════════════════════════════════════════════════════════════════════════╗
║                      POS SYSTEM - DTO FLOW ARCHITECTURE                          ║
╚══════════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────────┐
│  FRONTEND (Vue.js)                                                              │
│  ├── Sends JSON Request                                                         │
│  └── Receives JSON Response                                                     │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│  LAYER 1: CONTROLLER (@RestController)                                         │
│  ├── Receives HTTP Request                                                     │
│  ├── Maps JSON → Request DTO                                                   │
│  ├── Validates using @Valid                                                    │
│  └── Returns ApiResponse<ResponseDTO>                                          │
│                                                                                 │
│  Example:                                                                       │
│  @PostMapping("/api/orders")                                                   │
│  public ResponseEntity<ApiResponse<OrderResponse>> createOrder(                │
│      @Valid @RequestBody OrderRequest request)                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│  LAYER 2: REQUEST DTO (Validation Layer)                                       │
│  ├── OrderRequest.java                                                         │
│  ├── @NotNull, @NotBlank validations                                           │
│  ├── Contains ONLY fields needed for creation                                  │
│  └── Never exposed to database directly                                        │
│                                                                                 │
│  Fields: branchId, cashierUserId, orderType, items[]                           │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│  LAYER 3: SERVICE (@Service - Business Logic)                                  │
│  ├── Receives Request DTO                                                      │
│  ├── Validates business rules                                                  │
│  ├── Fetches related entities from Repository                                  │
│  ├── Maps Request DTO → Entity                                                 │
│  ├── Calls Repository.save()                                                   │
│  ├── Maps Entity → Response DTO                                                │
│  └── Returns Response DTO                                                      │
│                                                                                 │
│  Example:                                                                       │
│  OrderEntity order = new OrderEntity();                                        │
│  order.setBranch(branchRepository.findById(...));                              │
│  OrderEntity saved = orderRepository.save(order);                              │
│  return mapToResponse(saved);                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│  LAYER 4: REPOSITORY (@Repository - Data Access)                               │
│  ├── JpaRepository interface                                                   │
│  ├── Provides CRUD operations                                                  │
│  ├── Custom query methods                                                      │
│  └── Returns Entity objects                                                    │
│                                                                                 │
│  Example:                                                                       │
│  public interface OrderRepository extends JpaRepository<OrderEntity, Long> {   │
│      List<OrderEntity> findByBranch_BranchId(Long branchId);                   │
│  }                                                                              │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│  LAYER 5: ENTITY (@Entity - Database Model)                                    │
│  ├── OrderEntity.java                                                          │
│  ├── @OneToMany, @ManyToOne relationships                                      │
│  ├── Mapped to database table                                                  │
│  └── NEVER sent to frontend                                                    │
│                                                                                 │
│  Fields: orderId, orderNo, branch, cashierUser, items, timestamps              │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│  LAYER 6: DATABASE (PostgreSQL/MySQL)                                          │
│  ├── Stores actual data                                                        │
│  ├── Tables: tblorders, tblorder_items, etc.                                   │
│  └── Managed by JPA/Hibernate                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘

╔══════════════════════════════════════════════════════════════════════════════════╗
║                            RETURN FLOW (Response)                                ║
╚══════════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────────┐
│  DATABASE                                                                       │
│  Returns: OrderEntity with all relationships                                    │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│  REPOSITORY                                                                     │
│  Returns: OrderEntity                                                           │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│  SERVICE - MAPPING LAYER (Critical!)                                           │
│  ├── Receives: OrderEntity (with all relationships)                            │
│  ├── Extracts safe data only                                                   │
│  ├── Flattens relationships (e.g., branch → branchId, branchName)              │
│  └── Returns: OrderResponse                                                    │
│                                                                                 │
│  Example:                                                                       │
│  OrderResponse response = new OrderResponse();                                 │
│  response.setOrderId(entity.getOrderId());                                     │
│  response.setBranchName(entity.getBranch().getName()); // ← Flatten            │
│  response.setCashierName(entity.getCashierUser()                               │
│      .getEmployee().getFirstName()); // ← Safe access                          │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│  RESPONSE DTO (Clean Data for Frontend)                                        │
│  ├── OrderResponse.java                                                        │
│  ├── Contains IDs, names, timestamps                                           │
│  ├── No entity references                                                      │
│  └── No sensitive data (passwords, etc.)                                       │
│                                                                                 │
│  Fields: orderId, orderNo, branchName, cashierName, items[], totalAmount       │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│  CONTROLLER                                                                     │
│  Wraps in ApiResponse<OrderResponse>                                           │
│  Returns HTTP 200/201 with JSON                                                │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│  FRONTEND                                                                       │
│  Receives clean JSON response                                                  │
│  {                                                                              │
│    "success": true,                                                             │
│    "message": "Order created",                                                  │
│    "data": { "orderId": 123, "orderNo": "ORD-001", ... }                       │
│  }                                                                              │
└─────────────────────────────────────────────────────────────────────────────────┘

╔══════════════════════════════════════════════════════════════════════════════════╗
║                          DTO FOLDER STRUCTURE                                    ║
╚══════════════════════════════════════════════════════════════════════════════════╝

dto/
├── common/                    🌐 Shared wrappers
│   ├── ApiResponse.java      → Wraps all API responses
│   └── PageResponse.java     → Pagination support
│
├── user/                      👤 User & Auth
│   ├── UserRequest.java      → Create/update user
│   ├── UserResponse.java     → User data (no password!)
│   ├── LoginRequest.java     → Login credentials
│   └── LoginResponse.java    → JWT token + user info
│
├── order/                     🛒 Order Management (Core POS)
│   ├── OrderRequest.java     → Create order
│   ├── OrderResponse.java    → Order details
│   ├── OrderItemRequest.java → Nested: order line item
│   └── OrderItemResponse.java→ Item in order
│
├── menu/                      📋 Menu Items
│   ├── MenuItemRequest.java  → Add/edit menu item
│   └── MenuItemResponse.java → Menu item data
│
├── category/                  📂 Categories
│   ├── CategoryRequest.java
│   └── CategoryResponse.java
│
├── customer/                  👥 Customers
│   ├── CustomerRequest.java
│   └── CustomerResponse.java
│
├── payment/                   💳 Payments
│   ├── PaymentRequest.java   → Process payment
│   └── PaymentResponse.java  → Payment result
│
└── pos/                       🏪 POS Specific (Keep existing)
    ├── Cashier.java
    ├── Product.java
    ├── SaleItem.java
    └── SalesRecord.java

╔══════════════════════════════════════════════════════════════════════════════════╗
║                         WHY USE DTOs? (Benefits)                                 ║
╚══════════════════════════════════════════════════════════════════════════════════╝

1. SECURITY 🔒
   ❌ Entity: Exposes all fields including passwords, internal IDs
   ✅ DTO: Only safe fields exposed

2. PERFORMANCE ⚡
   ❌ Entity: Lazy loading can cause N+1 queries in JSON serialization
   ✅ DTO: Explicit data, no lazy loading issues

3. VERSIONING 📌
   ❌ Entity: Changes affect database AND API
   ✅ DTO: API version independent of database

4. VALIDATION ✔️
   ❌ Entity: Database constraints only
   ✅ DTO: Request validation before hitting database

5. CLEAN API 🎨
   ❌ Entity: Complex nested objects in JSON
   ✅ DTO: Flat, clean JSON structure

╔══════════════════════════════════════════════════════════════════════════════════╗
║                    KEY PRINCIPLE (Most Important!)                               ║
╚══════════════════════════════════════════════════════════════════════════════════╝

    🚫 NEVER RETURN ENTITY FROM CONTROLLER 🚫
    
    ❌ return orderEntity;           // BAD - exposes everything
    ❌ return userEntity;             // BAD - includes password!
    
    ✅ return orderResponse;         // GOOD - safe data only
    ✅ return userResponse;          // GOOD - no password

    📜 Always: Entity ──[Service Maps]──► Response DTO ──► Frontend

═══════════════════════════════════════════════════════════════════════════════════

                      🎉 DTO Structure Complete! 🎉
                      
            Your POS system now follows Spring Boot best practices
                with clean separation of concerns!

═══════════════════════════════════════════════════════════════════════════════════
```
